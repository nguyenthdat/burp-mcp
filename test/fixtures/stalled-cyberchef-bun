#!/bin/sh
COUNT_FILE="$(dirname "$0")/.stalled-cyberchef-count"
if [ -f "$COUNT_FILE" ]; then
  exec bun "$(dirname "$0")/stalled-cyberchef-worker.mjs" --replacement
fi
: > "$COUNT_FILE"
exec bun "$(dirname "$0")/stalled-cyberchef-worker.mjs"
